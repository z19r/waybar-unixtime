[package]
name = "waybar-unixtime"
version = "0.4.0"
edition = "2021"
rust-version = "1.74"
description = "Live unix timestamps for Waybar, themed by your omarchy theme"
license = "MIT"
repository = "https://github.com/z19r/waybar-unixtime"
homepage = "https://github.com/z19r/waybar-unixtime"
keywords = ["waybar", "hyprland", "unix-time", "omarchy", "statusbar"]
categories = ["command-line-utilities", "date-and-time"]
readme = "README.md"

[[bin]]
name = "waybar-unixtime"
path = "src/main.rs"

[dependencies]
chrono = { version = "0.4", default-features = false, features = [
  "clock",
  "std",
] }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
signal-hook = "0.3"
toml = "0.8"

[dev-dependencies]
assert_cmd = "2"

[profile.release]
lto = true
strip = true
codegen-units = 1
